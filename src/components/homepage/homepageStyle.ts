import { StyleSheet } from "react-native";

export const homepageStyle = StyleSheet.create({
  wrapper: {
    gap: 20,
  },

  friendsContainer: {
    height: 20,
  },

  title: {
    textAlign: "left",
    fontSize: 25,
  },
});
