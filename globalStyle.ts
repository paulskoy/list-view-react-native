import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
  wrapper: {
    flex: 1,
    backgroundColor: "black",
    padding: 20,
  },

  text: {
    color: "white",
    fontSize: 16,
    fontWeight: "bold",
  },
});
